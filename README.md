BAGGAGE GUARDIAN

This zip contains scaffolding for:
- backend/  FastAPI + YOLOv8 + OpenCV hybrid service
- frontend/ React + TypeScript UI with overlays + PDF reporting

NOTE:
Due to chat packaging limits, full source listings are in conversation messages.
Copy provided code into proper files per structure.


cd BaggageGuardian/backend
pip install -r requirements.txt
uvicorn main:app --reload


cd BaggageGuardian/frontend
npm install    # only first time
npm run dev  

